{{#if (eq auth "better-auth")}}
export * from "./auth";
{{/if}}
{{#if (includes examples "todo")}}
export * from "./todo";
{{/if}}
{{#unless (or (eq auth "better-auth") (includes examples "todo"))}}
export {};
{{/unless}}